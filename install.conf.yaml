- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.antigenrc:
    ~/.agignore:
    ~/.aria2/aria2.conf:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.config/i3/config: i3config
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.gnupg/gpg.conf:
    ~/.i3status.conf:
    ~/.pythonrc:
    ~/.screenrc:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - mkdir -p ~/bin
  # the following have to run after linking ~/.gitconfig
  - git update-submodules